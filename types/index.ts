export type NFTProps = {
  selectedNft: {
    id: string;
    name: string;
    image: string;
    description: string;
    background_color: string;
    uri: string;
    external_url: string;
  };
};
